@startuml
title 第 7 章示意图：跨站补料与空箱回收的流程图版本

skinparam shadowing false
skinparam defaultTextAlignment center
skinparam wrapWidth 220

start

:进入跨站补料与空箱回收任务;
:在 A 仓位取连接器料箱;
:向 3 号装配站导航;
:持续读取站点状态、电量和路线状态;

if (3 号站可立即对接?) then (是)
  if (电量足够直接完成交付与回收?) then (是)
    :到站交付料箱;
    :回收空箱;
    :记录任务完成;
    stop
  else (否)
    :前往充电点补能;
    :补能完成后重新规划路线;
    :继续前往 3 号站;
    :到站交付料箱;
    :回收空箱;
    :记录任务完成;
    stop
  endif
else (否)
  :前往临时缓存位等待;

  if (等待期间电量低于阈值?) then (是)
    :前往充电点补能;
  endif

  :重新获取站点和路线状态;

  if (3 号站可对接且路线可达?) then (是)
    :重新规划路线;
    :到站交付料箱;
    :回收空箱;
    :记录任务完成;
    stop
  else (否)
    :转人工调度;
    stop
  endif
endif

note right
这张图适合说明流程，
但一旦继续叠加下面这些维度，
图会迅速变长：

- 多站点切换
- 缓存位不可用
- 路线临时禁行
- 多次补能判断
- 多个恢复子流程复用

这正是后面更适合
换成行为树表达的原因。
end note

@enduml
