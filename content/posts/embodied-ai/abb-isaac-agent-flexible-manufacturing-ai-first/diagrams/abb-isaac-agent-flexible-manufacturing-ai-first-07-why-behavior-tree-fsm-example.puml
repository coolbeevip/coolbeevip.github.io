@startuml
title 第 7 章示意图：FPC 精密插装失败后的状态机版本

skinparam shadowing false
skinparam defaultTextAlignment center
skinparam wrapWidth 220
hide empty description

[*] --> 首次插入中

首次插入中 --> 插入成功 : 插入完成
首次插入中 --> 读取状态快照 : 第一次插入失败

读取状态快照 --> 等待人工确认 : 不允许自动恢复
读取状态快照 --> 低速重拍中 : 允许自动恢复

低速重拍中 --> 等待视觉结果 : 重拍完成
等待视觉结果 --> 偏移可补偿 : offset <= 0.3 mm
等待视觉结果 --> 偏移不可补偿 : offset > 0.3 mm

偏移可补偿 --> 二次对准中 : 开始对准
二次对准中 --> 第二次插入中 : 对准完成

第二次插入中 --> 恢复成功 : 第二次插入成功
第二次插入中 --> 自动恢复失败 : 第二次插入失败

偏移不可补偿 --> 回安全位中 : 规则要求先回安全位
偏移不可补偿 --> 等待人工确认 : 可直接人工接管

自动恢复失败 --> 回安全位中 : 规则要求先回安全位
自动恢复失败 --> 等待人工确认 : 可直接人工接管

回安全位中 --> 等待人工确认 : 回位完成

插入成功 --> [*]
恢复成功 --> [*]
等待人工确认 --> [*]

note right of 读取状态快照
在状态机里，
很多本来只是判断条件的东西，
会被抬成后续状态切换的入口。
end note

note right of 等待视觉结果
如果继续叠加：

- 第几次重试
- 视觉超时
- PLC 互锁变化
- 不同产品配方
- 多条恢复路径

通常就要继续新增状态
或新增大量转移条件。
end note

note bottom of 自动恢复失败
状态机当然能表达这个场景。
问题不在能不能做，
而在复杂度升高后，
状态和转移会明显膨胀。
end note

note bottom of 回安全位中
这也是工业里常见的分工：

- 控制器 / PLC 内部状态机
  很适合设备级确定性逻辑
- 任务级恢复闭环
  一旦开始强调复用、局部回退、
  条件反复检查和子流程复用，
  行为树通常会更顺手
end note

@enduml
