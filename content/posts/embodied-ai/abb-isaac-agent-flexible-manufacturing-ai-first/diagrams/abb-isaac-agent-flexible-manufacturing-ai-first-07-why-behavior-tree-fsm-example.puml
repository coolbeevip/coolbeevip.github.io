@startuml
title 第 7 章示意图：跨站补料与空箱回收的状态机版本

skinparam shadowing false
skinparam defaultTextAlignment center
skinparam wrapWidth 220
hide empty description

[*] --> 去A仓位取料中

去A仓位取料中 --> 去3号站中 : 取料完成

去3号站中 --> 到站交付中 : 站点可对接 && 电量充足
去3号站中 --> 去缓存位中 : 站点占用
去3号站中 --> 去充电点中 : 电量低于阈值

去缓存位中 --> 等待站点释放中 : 到达缓存位
去缓存位中 --> 等待人工调度 : 缓存位不可用

等待站点释放中 --> 重新规划中 : 站点已释放 && 路线可达
等待站点释放中 --> 去充电点中 : 等待期间电量低于阈值
等待站点释放中 --> 等待人工调度 : 长时间占用或路线不可达

去充电点中 --> 充电中 : 到达充电点
充电中 --> 重新规划中 : 电量恢复可继续任务

重新规划中 --> 去3号站中 : 新路线生成
重新规划中 --> 等待人工调度 : 无可行路线

到站交付中 --> 回收空箱中 : 交付完成
回收空箱中 --> 任务完成 : 回收完成

任务完成 --> [*]
等待人工调度 --> [*]

note right of 去3号站中
在状态机里，
很多本来只是判断条件的东西，
会被抬成后续状态切换的入口。
end note

note right of 等待站点释放中
如果继续叠加：

- 第几个目标站
- 当前剩余电量档位
- 缓存位是否可用
- 路线是否禁行
- 是否允许跳站或改站

通常就要继续新增状态
或新增大量转移条件。
end note

note bottom of 重新规划中
状态机当然能表达这个场景。
问题不在能不能做，
而在复杂度升高后，
状态和转移会明显膨胀。
end note

note bottom of 去充电点中
这也是工业里常见的分工：

- 导航、充电、对接等底层模块
  很适合设备级确定性状态机
- 跨站补料这种任务级闭环
  一旦开始强调复用、局部恢复、
  条件反复检查和子流程复用，
  行为树通常会更顺手
end note

@enduml
